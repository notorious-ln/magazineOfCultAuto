<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Кастомный стиль для кнопок -->
    <style name="CustomButtonStyle1" parent="Widget.MaterialComponents.Button">
        <!-- Базовые параметры -->
        <item name="android:minWidth">120dp</item>
        <item name="android:minHeight">60dp</item>

        <!-- Цвета -->
        <item name="backgroundTint">#50C878</item> <!-- Изумрудный цвет -->
        <item name="android:textColor">#000000</item>

        <!-- Текст -->
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:includeFontPadding">false</item>

        <!-- Отступы (совместимый вариант) -->
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <!-- Или для RTL поддержки: -->
        <!--
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        -->

        <!-- Границы -->
        <item name="strokeColor">#000000</item>
        <item name="strokeWidth">1dp</item>
        <item name="cornerRadius">12dp</item> <!-- Скругленные углы -->

    </style>
    <!-- Кастомный стиль для кнопок с градиентом -->
    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <!-- Базовые параметры -->
        <item name="android:minWidth">120dp</item>
        <item name="android:minHeight">60dp</item>

        <!-- Текст -->
        <item name="android:textColor">#FFFFFF</item> <!-- Белый текст для контраста -->
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:includeFontPadding">false</item>

        <!-- Отступы -->
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>

        <!-- Градиентный фон -->
        <item name="android:background">@drawable/gradient_background</item>
        <item name="backgroundTint">@null</item> <!-- Отключаем tint, чтобы работал наш градиент -->

        <!-- Границы -->
        <item name="strokeColor">#FFFFFF</item> <!-- Белая обводка -->
        <item name="strokeWidth">1dp</item>
        <item name="cornerRadius">12dp</item> <!-- Скругленные углы -->
    </style>
    <!-- Стиль для кнопок выбора размера -->
    <style name="SizeButtonStyle" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginStart">4dp</item> <!-- Замена -->
        <item name="android:layout_marginEnd">4dp</item> <!-- Замена -->
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:gravity">center</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:padding">0dp</item>
        <item name="cornerRadius">24dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">#000000</item>
        <item name="backgroundTint">@color/size_button_tint</item>
    </style>
</resources>